{{- if .Values.oauth2_proxy.enabled -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  labels:
    app: nexus-proxy
{{- include "nexus-operator.labels" . | nindent 4 }}
  name: nexus-proxy
{{- end -}}
