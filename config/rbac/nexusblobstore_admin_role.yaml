apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: nexus-operator
    app.kubernetes.io/managed-by: kustomize
  name: nexusblobstore-admin-role
rules:
- apiGroups:
  - edp.epam.com
  resources:
  - nexusblobstores
  verbs:
  - '*'
- apiGroups:
  - edp.epam.com
  resources:
  - nexusblobstores/status
  verbs:
  - get
