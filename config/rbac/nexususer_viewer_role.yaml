# permissions for end users to view nexususers.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: clusterrole
    app.kubernetes.io/instance: nexususer-viewer-role
    app.kubernetes.io/component: rbac
    app.kubernetes.io/created-by: controller-operator
    app.kubernetes.io/part-of: controller-operator
    app.kubernetes.io/managed-by: kustomize
  name: nexususer-viewer-role
rules:
- apiGroups:
  - v1.edp.epam.com
  resources:
  - nexususers
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - v1.edp.epam.com
  resources:
  - nexususers/status
  verbs:
  - get
